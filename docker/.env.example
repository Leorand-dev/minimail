# Minimail Docker 部署配置
# 复制此文件为 .env 并按需修改

# PostgreSQL 密码
DB_PASSWORD=your_strong_password

# JWT 签名密钥 (生成: openssl rand -hex 32)
SECRET_KEY=

# IMAP/SMTP 密码加密密钥 (生成: openssl rand -base64 32 | cut -c1-32)
ENCRYPTION_KEY=

# 部署域名 (用于 CORS)
DOMAIN=localhost

# Redis 密码 (可选)
# REDIS_PASSWORD=
